import styled from 'styled-components'

export const Widget = styled.div`
  margin-top: 2rem;
  width: 15rem;

  a {
    color: inherit;
  }

  @media (max-width: 45rem) {
    min-width: 15rem;
  }
`
