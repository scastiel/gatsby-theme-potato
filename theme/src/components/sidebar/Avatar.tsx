import styled from 'styled-components'

export const Avatar = styled.img`
  --size: 5em;
  width: var(--size);
  height: var(--size);
  border-radius: 50%;
  margin-bottom: 1rem;
  background-color: var(--accentColor);
`
